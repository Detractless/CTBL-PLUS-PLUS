<Window x:Class="CtblPlusPlus.Installer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="CTBL Queue Delay Setup"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="#18181B">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Slim fixed header (no drag: window is maximized full-screen) -->
        <Grid Grid.Row="0" Background="#101012">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <Rectangle Width="3" Height="16" Fill="#65A30D" Margin="0,0,12,0" />
                <TextBlock Text="CTBL QUEUE DELAY · SETUP" Foreground="#A1A1AA"
                           VerticalAlignment="Center" FontFamily="Segoe UI" FontSize="12"
                           FontWeight="SemiBold" />
            </StackPanel>

            <Button x:Name="CloseBtn" Grid.Column="1" Width="46" Height="40"
                    Background="Transparent" BorderThickness="0" Foreground="#A1A1AA"
                    Content="&#x2715;" Click="CloseButton_Click" ToolTip="Cancel Setup"
                    Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" />
        </Grid>

        <wv2:WebView2 Grid.Row="1" Name="webView" />
    </Grid>
</Window>
